<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="WinClock.SettingsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinClock"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Settings">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Pivot x:Name="SettingsPivot">
        <Pivot.Resources>
            <local:ColorToBrushConverter x:Key="ColorToBrushConverter"/>
        </Pivot.Resources>
        <!-- General Settings Tab -->
        <PivotItem x:Name="GeneralTab" Header="General">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="SettingsStackPanel" Padding="20" Spacing="15">
                    <TextBlock x:Name="SettingsHeader" Text="Settings" FontSize="20" FontWeight="SemiBold"/>
                    
                    <StackPanel>
                        <TextBlock x:Name="LanguageLabel" Text="Language" Style="{StaticResource CaptionTextBlockStyle}"/>
                        <ComboBox x:Name="LanguageComboBox" SelectionChanged="LanguageComboBox_SelectionChanged" HorizontalAlignment="Stretch">
                            <ComboBoxItem Content="English" Tag="en"/>
                            <ComboBoxItem Content="עברית" Tag="he"/>
                            <ComboBoxItem Content="Español" Tag="es"/>
                            <ComboBoxItem Content="Français" Tag="fr"/>
                            <ComboBoxItem Content="Deutsch" Tag="de"/>
                            <ComboBoxItem Content="Italiano" Tag="it"/>
                            <ComboBoxItem Content="Português" Tag="pt"/>
                            <ComboBoxItem Content="Русский" Tag="ru"/>
                            <ComboBoxItem Content="日本語" Tag="ja"/>
                            <ComboBoxItem Content="한국어" Tag="ko"/>
                            <ComboBoxItem Content="中文 (简体)" Tag="zh"/>
                            <ComboBoxItem Content="हिन्दी" Tag="hi"/>
                            <ComboBoxItem Content="العربية" Tag="ar"/>
                            <ComboBoxItem Content="Türkçe" Tag="tr"/>
                            <ComboBoxItem Content="Tiếng Việt" Tag="vi"/>
                            <ComboBoxItem Content="Polski" Tag="pl"/>
                            <ComboBoxItem Content="Nederlands" Tag="nl"/>
                            <ComboBoxItem Content="Bahasa Indonesia" Tag="id"/>
                            <ComboBoxItem Content="ไทย" Tag="th"/>
                            <ComboBoxItem Content="Ελληνικά" Tag="el"/>
                            <ComboBoxItem Content="Română" Tag="ro"/>
                            <ComboBoxItem Content="Magyar" Tag="hu"/>
                        </ComboBox>
                    </StackPanel>

                    <StackPanel>
                        <TextBlock x:Name="TimeFormatLabel" Text="Time Format" Style="{StaticResource CaptionTextBlockStyle}"/>
                        <ComboBox x:Name="TimeFormatComboBox" SelectionChanged="TimeFormatComboBox_SelectionChanged" HorizontalAlignment="Stretch">
                            <ComboBoxItem Content="None" Tag="None"/>
                            <ComboBoxItem Content="HH:mm:ss" Tag="HH:mm:ss"/>
                            <ComboBoxItem Content="HH:mm" Tag="HH:mm"/>
                        </ComboBox>
                    </StackPanel>

                    <ToggleSwitch x:Name="TimeFormatToggle" Header="24-Hour Format" IsOn="True" Toggled="TimeFormatToggle_Toggled"/>

                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <StackPanel Width="150">
                            <TextBlock x:Name="TimeFontSizeLabel" Text="Time Font Size" Style="{StaticResource CaptionTextBlockStyle}"/>
                            <Slider x:Name="TimeFontSizeSlider" Minimum="20" Maximum="120" Value="72" ValueChanged="TimeFontSizeSlider_ValueChanged"/>
                        </StackPanel>
                        <StackPanel Width="150">
                            <TextBlock x:Name="TimeFontFamilyLabel" Text="Time Font" Style="{StaticResource CaptionTextBlockStyle}"/>
                            <ComboBox x:Name="TimeFontFamilyComboBox" SelectionChanged="TimeFontFamilyComboBox_SelectionChanged" HorizontalAlignment="Stretch">
                                <ComboBoxItem Content="Segoe UI" Tag="Segoe UI Variable"/>
                                <ComboBoxItem Content="Arial" Tag="Arial"/>
                                <ComboBoxItem Content="Calibri" Tag="Calibri"/>
                                <ComboBoxItem Content="Verdana" Tag="Verdana"/>
                            </ComboBox>
                        </StackPanel>
                    </StackPanel>

                    <StackPanel>
                        <TextBlock x:Name="TimeColorLabel" Text="Time Color" Style="{StaticResource CaptionTextBlockStyle}"/>
                        <Button x:Name="TimeColorButton" HorizontalAlignment="Stretch">
                            <Button.Flyout>
                                <Flyout>
                                    <ColorPicker x:Name="TimeColorPicker" ColorChanged="TimeColorPicker_ColorChanged" IsAlphaEnabled="True" IsHexInputVisible="True" Height="300"/>
                                </Flyout>
                            </Button.Flyout>
                            <Rectangle Fill="{Binding ElementName=TimeColorPicker, Path=Color, Converter={StaticResource ColorToBrushConverter}, FallbackValue=Red}" Height="20" Width="100"/>
                        </Button>
                    </StackPanel>

                    <StackPanel>
                        <TextBlock x:Name="DateFormatLabel" Text="Date Format" Style="{StaticResource CaptionTextBlockStyle}"/>
                        <ComboBox x:Name="DateFormatComboBox" SelectionChanged="DateFormatComboBox_SelectionChanged" HorizontalAlignment="Stretch">
                            <ComboBoxItem Content="None" Tag="None"/>
                            <ComboBoxItem Content="dd/MM/yyyy" Tag="dd/MM/yyyy"/>
                            <ComboBoxItem Content="MM/dd/yyyy" Tag="MM/dd/yyyy"/>
                        </ComboBox>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <StackPanel Width="150">
                            <TextBlock x:Name="DateFontSizeLabel" Text="Date Font Size" Style="{StaticResource CaptionTextBlockStyle}"/>
                            <Slider x:Name="DateFontSizeSlider" Minimum="10" Maximum="60" Value="18" ValueChanged="DateFontSizeSlider_ValueChanged"/>
                        </StackPanel>
                        <StackPanel Width="150">
                            <TextBlock x:Name="DateFontFamilyLabel" Text="Date Font" Style="{StaticResource CaptionTextBlockStyle}"/>
                            <ComboBox x:Name="DateFontFamilyComboBox" SelectionChanged="DateFontFamilyComboBox_SelectionChanged" HorizontalAlignment="Stretch">
                                <ComboBoxItem Content="Segoe UI" Tag="Segoe UI Variable"/>
                                <ComboBoxItem Content="Arial" Tag="Arial"/>
                            </ComboBox>
                        </StackPanel>
                    </StackPanel>

                    <StackPanel>
                        <TextBlock x:Name="DateColorLabel" Text="Date Color" Style="{StaticResource CaptionTextBlockStyle}"/>
                        <Button x:Name="DateColorButton" HorizontalAlignment="Stretch">
                            <Button.Flyout>
                                <Flyout>
                                    <ColorPicker x:Name="DateColorPicker" ColorChanged="DateColorPicker_ColorChanged" IsAlphaEnabled="True" IsHexInputVisible="True" Height="300"/>
                                </Flyout>
                            </Button.Flyout>
                            <Rectangle Fill="{Binding ElementName=DateColorPicker, Path=Color, Converter={StaticResource ColorToBrushConverter}, FallbackValue=White}" Height="20" Width="100"/>
                        </Button>
                    </StackPanel>

                    <StackPanel Spacing="5">
                        <TextBlock x:Name="BackgroundLabel" Text="Background Image" Style="{StaticResource CaptionTextBlockStyle}"/>
                        <Button x:Name="SelectBackgroundButton" Content="Select Image" Click="SelectBackgroundButton_Click" HorizontalAlignment="Stretch"/>
                        <Button x:Name="ClearBackgroundButton" Content="Clear Background" Click="ClearBackgroundButton_Click" HorizontalAlignment="Stretch"/>
                    </StackPanel>

                    <StackPanel Spacing="5">
                        <StackPanel>
                            <TextBlock x:Name="WindowWidthLabel" Text="Window Width" Style="{StaticResource CaptionTextBlockStyle}"/>
                            <Slider x:Name="WindowWidthSlider" Minimum="200" Maximum="1200" Value="450" ValueChanged="WindowWidthSlider_ValueChanged"/>
                        </StackPanel>
                        <StackPanel>
                            <TextBlock x:Name="WindowHeightLabel" Text="Window Height" Style="{StaticResource CaptionTextBlockStyle}"/>
                            <Slider x:Name="WindowHeightSlider" Minimum="150" Maximum="1000" Value="300" ValueChanged="WindowHeightSlider_ValueChanged"/>
                        </StackPanel>
                    </StackPanel>

                    <ToggleSwitch x:Name="AlwaysOnTopToggle" Header="Always on Top" IsOn="True" Toggled="AlwaysOnTopToggle_Toggled"/>

                    <!-- Hebrew Date Toggle -->
                    <CheckBox x:Name="HebrewDateCheckBox" Content="Show Hebrew Date" Checked="HebrewDateCheckBox_Changed" Unchecked="HebrewDateCheckBox_Changed"/>
                    
                    <!-- Hebrew Date Settings -->
                    <StackPanel x:Name="HebrewDateSettingsPanel" Spacing="10" Visibility="Collapsed" Margin="25,0,0,0">
                        <StackPanel Spacing="5">
                            <TextBlock x:Name="HebrewDateTransitionLabel" Text="Change Hebrew Date at (Tzet HaKochavim)" Style="{StaticResource CaptionTextBlockStyle}"/>
                            <TimePicker x:Name="HebrewDateTransitionTimePicker" SelectedTimeChanged="HebrewDateTransitionTimePicker_SelectedTimeChanged" HorizontalAlignment="Stretch"/>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Spacing="10">
                            <StackPanel Width="150">
                                <TextBlock x:Name="HebrewDateFontSizeLabel" Text="Hebrew Date Font Size" Style="{StaticResource CaptionTextBlockStyle}"/>
                                <Slider x:Name="HebrewDateFontSizeSlider" Minimum="10" Maximum="60" Value="16" ValueChanged="HebrewDateFontSizeSlider_ValueChanged"/>
                            </StackPanel>
                            <StackPanel Width="150">
                                <TextBlock x:Name="HebrewDateFontFamilyLabel" Text="Hebrew Date Font" Style="{StaticResource CaptionTextBlockStyle}"/>
                                <ComboBox x:Name="HebrewDateFontFamilyComboBox" SelectionChanged="HebrewDateFontFamilyComboBox_SelectionChanged" HorizontalAlignment="Stretch">
                                    <ComboBoxItem Content="Segoe UI" Tag="Segoe UI Variable"/>
                                    <ComboBoxItem Content="Arial" Tag="Arial"/>
                                </ComboBox>
                            </StackPanel>
                        </StackPanel>

                        <StackPanel>
                            <TextBlock x:Name="HebrewDateColorLabel" Text="Hebrew Date Color" Style="{StaticResource CaptionTextBlockStyle}"/>
                            <Button x:Name="HebrewDateColorButton" HorizontalAlignment="Stretch">
                                <Button.Flyout>
                                    <Flyout>
                                        <ColorPicker x:Name="HebrewDateColorPicker" ColorChanged="HebrewDateColorPicker_ColorChanged" IsAlphaEnabled="True" IsHexInputVisible="True" Height="300"/>
                                    </Flyout>
                                </Button.Flyout>
                                <Rectangle Fill="{Binding ElementName=HebrewDateColorPicker, Path=Color, Converter={StaticResource ColorToBrushConverter}, FallbackValue=LightGray}" Height="20" Width="100"/>
                            </Button>
                        </StackPanel>
                    </StackPanel>

                    
                    <Button x:Name="CloseButton" Content="Close" Click="CloseButton_Click" HorizontalAlignment="Right"/>
                </StackPanel>
            </ScrollViewer>
        </PivotItem>

        <!-- Alarms Tab -->
        <PivotItem x:Name="AlarmsTab" Header="Alarms">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel Spacing="15" Padding="20">
                    <TextBlock x:Name="AlarmsHeader" Text="Alarms (Max 10)" Style="{StaticResource SubtitleTextBlockStyle}"/>
                    
                    <StackPanel Spacing="5">
                        <TextBlock x:Name="AutoDismissLabel" Text="Auto-Dismiss Timeout (sec)" Style="{StaticResource CaptionTextBlockStyle}"/>
                        <TextBox x:Name="AutoDismissTextBox" Text="60" TextChanged="AutoDismissTextBox_TextChanged" PlaceholderText="0 = Never dismiss"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <StackPanel Width="150">
                            <TextBlock x:Name="AlertLabelFontSizeLabel" Text="Alert Font Size" Style="{StaticResource CaptionTextBlockStyle}"/>
                            <Slider x:Name="AlertLabelFontSizeSlider" Minimum="20" Maximum="120" Value="48" ValueChanged="AlertLabelFontSizeSlider_ValueChanged"/>
                        </StackPanel>
                        <StackPanel Width="150">
                            <TextBlock x:Name="AlertLabelFontFamilyLabel" Text="Alert Font" Style="{StaticResource CaptionTextBlockStyle}"/>
                            <ComboBox x:Name="AlertLabelFontFamilyComboBox" SelectionChanged="AlertLabelFontFamilyComboBox_SelectionChanged" HorizontalAlignment="Stretch">
                                <ComboBoxItem Content="Segoe UI" Tag="Segoe UI Variable"/>
                                <ComboBoxItem Content="Arial" Tag="Arial"/>
                                <ComboBoxItem Content="Impact" Tag="Impact"/>
                            </ComboBox>
                        </StackPanel>
                    </StackPanel>

                    <StackPanel>
                        <TextBlock x:Name="AlertLabelColorLabel" Text="Alert Color" Style="{StaticResource CaptionTextBlockStyle}"/>
                        <Button x:Name="AlertLabelColorButton" HorizontalAlignment="Stretch">
                            <Button.Flyout>
                                <Flyout>
                                    <ColorPicker x:Name="AlertLabelColorPicker" ColorChanged="AlertLabelColorPicker_ColorChanged" IsAlphaEnabled="True" IsHexInputVisible="True" Height="300"/>
                                </Flyout>
                            </Button.Flyout>
                            <Rectangle Fill="{Binding ElementName=AlertLabelColorPicker, Path=Color, Converter={StaticResource ColorToBrushConverter}, FallbackValue=Red}" Height="20" Width="100"/>
                        </Button>
                    </StackPanel>

                    <ScrollViewer MaxHeight="400" BorderBrush="{ThemeResource SystemControlForegroundBaseLowBrush}" BorderThickness="1">
                        <StackPanel x:Name="AlarmsListPanel" Spacing="5" Padding="10"/>
                    </ScrollViewer>

                    <StackPanel Spacing="10">
                        <TextBlock x:Name="AlarmLabelText" Text="Alarm Label"/>
                        <TextBox x:Name="NewAlarmLabelTextBox" HorizontalAlignment="Stretch" PlaceholderText="e.g. Wake up!"/>
                        <TextBlock x:Name="TimeLabel" Text="Time"/>
                        <TimePicker x:Name="NewAlarmTimePicker" HorizontalAlignment="Stretch"/>
                        <CheckBox x:Name="DailyCheckBox" Content="Daily" IsChecked="True" Checked="DailyCheckBox_Changed" Unchecked="DailyCheckBox_Changed"/>
                        <StackPanel x:Name="SpecificDatePanel" Visibility="Collapsed">
                            <TextBlock x:Name="SpecificDateLabel" Text="Specific Date"/>
                            <DatePicker x:Name="NewAlarmDatePicker" HorizontalAlignment="Stretch"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Spacing="10">
                            <Button x:Name="AddAlarmButton" Content="Add Alarm" Click="AddAlarmButton_Click" HorizontalAlignment="Stretch" Style="{StaticResource AccentButtonStyle}" Width="150"/>
                            <Button x:Name="CancelEditButton" Content="Cancel" Click="CancelEditButton_Click" Visibility="Collapsed" Width="100"/>
                        </StackPanel>
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>
        </PivotItem>
    </Pivot>
</Window>
